# Shared package

This workspace is reserved for framework-independent shared types and research-domain logic. It intentionally contains no application code during the repository bootstrap milestone.
